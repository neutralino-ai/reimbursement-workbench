export type Material = { id: string; filename: string; role: string; sha256: string; legacyVerified: boolean; integrity: 'ok' | 'missing' | 'changed'; href: string };
export type ClaimBatch = {id:string;version:string;recordIDs:string[];purpose:string;sourceMaterialIDs:string[];status:'active'|'archived';totalCNY?:string;recordVersions?:Record<string,string>;createdAt:string;updatedAt:string};
export type Policy = { id: string; title: string; versionLabel: string; materialID: string; status: 'active' | 'reference' | 'superseded'; note: string; clauses: {id: string; topic: string; section: string; pdfPage: number; printedPage?: string; quote: string; interpretation: string; scope: string}[]; version: string; material: Material | null; integrity: 'ok' | 'missing' | 'changed'; createdAt: string; updatedAt: string };
export type ExchangeRate = { date: string; currency: string; provider: 'BOC'; rateType: '中行折算价'; quotedRate: string; unit: 100; sourceUrl: string; evidenceIDs: string[]; cnyAmount: string; valid: boolean; issues: string[]; note?: string; recordedAt?: string; actor?: { type: string; id: string } };
export type RecordItem = { aiReview?:{id:string;status:string;at:string;reasons:string[];error:string}; version?: string; id: string; accountID: string; accountName: string; vendor: string; plan: string; date: string; billingMonth: string; invoiceNumber: string; amount: string; currency: string; claimedCNY: string; paymentVerified: boolean; claimConfirmed: boolean; submissionReference: string; submittedOn: string; notes: string; materials: Material[]; approvedCNY: string; outstandingCNY: string | null; status: 'needs_review' | 'ready' | 'submitted' | 'partial' | 'completed'; issues: string[]; exchangeRate?: ExchangeRate | null; financeReviewPending?: boolean; financeReviewEvidenceIDs?: string[]; financeReviewARPId?: string | null; priorStepsComplete?: boolean };
export type ARPItem = { reservedCNY?: string; reserveReason?: string; id: string; reimbursementNumber: string; summary: string; status: string; claimedCNY: string; approvedCNY: string; approvalVerified: boolean; availableCNY: string; sourceLabel: string; materialIds: string[]; observedAt?: string | null; financeReviewPending?: boolean; observationVerified?: boolean };
export type Allocation = { id: string; recordID: string; arpID: string; amountCNY: string; basis: string };
export type CardTransaction = { id: string; date: string; description: string; amount: string; currency: string; sourceFilename: string; candidateRecordIDs: string[] };
export type Source = { id: string; name: string; url: string; status: string; detail: string; checkedAt?: string; coverageFrom?: string; coverageTo?: string; evidenceIDs?: string[] };
export type GeneratedDocument = { submissionFormat?: 'integrated-v1' | 'separate-invoices-v1'; batchID?:string;batchVersion?:string; version?:string; id: string; title?: string; purpose?: 'review' | 'application'; status: string; stale?: boolean; recordIDs: string[]; sourceMaterialIDs: string[]; materialIDs: string[]; materials: Material[]; staleRecordIDs?: string[]; registeredAt?: string; note?: string; sourceRecords: { id: string; version: string }[]; submissionPDFMaterialID?: string; needsUpdate?: boolean; readinessIssues?: string[]; registeredStatus?: string; ready?: boolean };
export type DeliveryStatus = 'unknown' | 'submitted' | 'not_submitted';
export type DeliveryItem = { recordID: string; materialID: string; status: DeliveryStatus; effectiveStatus?: DeliveryStatus; completedByApproval?: boolean; completedByFinance?: boolean; financeReviewPending?: boolean; updatedAt: string | null; version: string; required?: boolean; note?: string; actor?: { type: string; id: string } | null };
export type Workspace = { claimBatches?:ClaimBatch[]; policies?: Policy[]; materials?: Material[]; documents?: GeneratedDocument[]; deliveryItems?: DeliveryItem[]; scope: string; records: RecordItem[]; arpRecords: ARPItem[]; allocations: Allocation[]; events: { id: string; at: string; type: string; summary: string }[]; importSummary: { accounts: number; records: number; deferredRecords: number; materials: number; importedAt: string }; sources: Source[]; gaps: { month: string; reason: string }[]; cardTransactions: CardTransaction[] };
