Noto Sans SC 中文字体用于服务端 PDF 字体嵌入，不随 Electron 客户端打包。

来源：https://github.com/google/fonts/tree/main/ofl/notosanssc

`NotoSansSC-Regular.ttf` 由官方 `NotoSansSC[wght].ttf` 用 fonttools 4.59.2 固定 `wght=400` 生成。许可为同目录 OFL.txt；字体不单独销售。ReportLab 按文档内容嵌入字体子集，客户端不需要安装本字体。
